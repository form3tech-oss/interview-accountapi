package accountValidator

import "fmt"

/*
	Function to validate fields in AccountData
	For example if value is negative or empty
*/
func Validate(accountData map[string]interface{}) bool {
	fmt.Println("\nValidating fields in Account:")
	return true
}
